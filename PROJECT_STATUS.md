# 보험브릿지 프로젝트 현재 상태

최종 업데이트: 2026-08-24


## 1. 프로젝트 개요

사이트명: 보험브릿지
도메인: insurancebridge.co.kr

보험 실무에 필요한 자료를 제공하는 웹사이트이며
주요 기능에는 다음이 포함된다.

- 보험사별 담보분류표
- 질병코드/수술분류 조회
- 보험 실무정보
- 분쟁조정/판례
- 보험 관련 검색 및 실무 기능

프로젝트는 GitHub를 통해 관리 및 배포되고 있다.


## 2. 가장 중요한 작업 원칙

보험 데이터는 프로젝트에서 가장 중요한 원본 데이터다.

AI가 사용자의 요청 없이 기존 보험 데이터를
임의 수정, 정리, 표준화, 축약, 삭제, 재정렬하면 안 된다.

특히 다음 항목은 사용자 명시 요청 없이는 변경 금지:

- 보험사명
- 담보명
- SubTab ID
- label
- groupCount
- 질병명
- 질병코드(KCD)
- 수술명
- 수술코드
- 그룹 구성
- section
- summary
- 배열 순서
- 약관 분류 내용

오탈자로 보이더라도 AI 판단으로 수정하지 않는다.

특정 보험사 작업을 요청받았다면
해당 보험사만 수정하고 다른 보험사는 절대 수정하지 않는다.

세부 보호 규칙은 반드시 `/AGENTS.md`를 먼저 읽고 따른다.


## 3. 보험 데이터 구조

보험사별 핵심 데이터는 주로 다음 위치에 있다.

`src/data/terms/`

현재 주요 보험사 데이터 파일:

- db.ts
- kb.ts
- meritz.ts
- hanwha.ts
- lotte.ts
- samsung.ts
- hyundai.ts
- heungkuk.ts
- nh.ts
- hana.ts

중앙 연결/메타정보:

`src/data/terms/index.ts`

주요 담보분류표 UI:

`src/components/TermsMaster.tsx`

보험사별 데이터는 개별 파일로 모듈화되어 있으며,
TermsMaster.tsx는 이를 참조하여 화면에 표시한다.

index.ts와 TermsMaster.tsx는 신규 담보 추가 과정에서
기존 데이터가 잘못 변경될 위험이 있으므로 특히 주의한다.


## 4. 보험 데이터 보호체계

2026-08-24 기준 보험 데이터 보호체계 구축 완료.

### AGENTS.md

프로젝트 루트에 `/AGENTS.md` 존재.

AI 작업 시 보험 데이터 수정 금지 및
Append-Only 원칙 등의 보호 규칙이 정의되어 있다.

AI는 작업 시작 전에 AGENTS.md를 우선 확인한다.


### 정상 백업 기준점

GitHub Release/Tag:

`insurance-data-stable-2026-08-24`

Release:

`보험 데이터 정상 기준점 2026-08-24`

이 기준점은 보험 데이터가 정상인 상태를
비교/복구하기 위한 기준점이다.

문제가 발생하면 현재 데이터를 임의 수정하지 말고
먼저 이 기준점과 diff를 비교한다.


## 5. 자동 보험 데이터 변경 감지

자동 변경 감지 시스템 구축 완료.

스크립트:

`scripts/check-insurance-data-changes.mjs`

GitHub Actions:

`.github/workflows/check-insurance-data.yml`

package.json에 보험 데이터 검사 명령이 등록되어 있다.

기본 원칙:

보험 데이터 변경은 기본적으로 차단한다.

특정 보험사 작업을 명시적으로 허용한 경우에만
해당 보험사 파일 변경을 허용한다.

예:

`ALLOWED_INSURANCE_DATA=lotte`

→ 롯데손해보험 데이터 변경 허용

이 상태에서 DB, KB 등 다른 보험사 파일까지 변경되면
검사 실패하도록 구성되어 있다.

복수 보험사 허용도 지원한다.

공통 데이터는 보험사 허용과 별도로 보호한다.


## 6. 검색엔진/SEO 현재 상태

Google Analytics GA4 설치 완료.

측정 ID:

`G-69KM9EF6S0`

실제 운영 사이트의 index.html 배포까지 확인 완료.


사이트명 SEO 개선 완료.

검색엔진이 사이트명을

`보험브릿지`

로 인식하도록 index.html에 WebSite 구조화 데이터 등
사이트명 관련 SEO 정보를 적용했다.

기준:

name: 보험브릿지
alternateName: Insurance Bridge
url: https://insurancebridge.co.kr/


## 7. 네이버 검색 상태

네이버에서 보험브릿지 세부 페이지가 검색 노출되기 시작했다.

예:
DB손해보험 통합암 관련 검색결과 노출 확인.

다만

`KB손해보험 분류표`

검색 시 DB손해보험 페이지가 노출되는 현상이 있었다.

이에 대해 코드 진단을 진행했으며:

- KB SEO 기술 오류 없음
- KB 페이지에 DB 데이터 혼입 없음
- canonical 정상
- title/description 정상
- 구조화 데이터 정상
- sitemap 등록 정상

으로 확인됨.

현재 판단:

`기술 오류 없음 / 네이버 색인 대기 가능성 높음`

따라서 이 현상을 이유로 KB 보험 데이터나 SEO 코드를
임의 수정하지 않는다.

네이버 재수집/색인 진행을 우선 기다린다.


## 8. Google Analytics

GA4 설치 및 실제 운영 사이트 배포 완료.

앞으로 설치 시점 이후부터:

- 일일 사용자
- 월간 사용자
- 세션
- 페이지 조회
- 인기 페이지
- 유입경로
- 신규/재방문
- 기기 등의 통계를 확인할 수 있다.

설치 이전 방문 데이터는 소급되지 않는다.


## 9. 최근 보험 데이터 복구 관련 중요 이력

과거 AI 작업 과정에서 보험사별 담보 SubTab,
label, groupCount 및 일부 데이터 구조가
사용자 의도와 다르게 변경되는 문제가 발생한 적이 있다.

특히 기존 정상 데이터 복구 과정에서
현재 파일을 과거 원본으로 잘못 가정하거나
ID/label을 추측하여 비교하는 문제가 있었다.

따라서 앞으로는:

- ID를 이름이 비슷하다는 이유로 추측하지 않는다.
- 과거 원본 확인 없이 복구하지 않는다.
- 사용자 자료 외 내용을 임의 추가하지 않는다.
- 정상 기준점 또는 실제 Git history를 기준으로 비교한다.
- 수정 전 diff 범위를 먼저 확인한다.
- 요청 범위 외 보험사 데이터는 수정하지 않는다.

이 규칙은 매우 중요하다.


## 10. 보험사별 담보분류표 작업 방식

앞으로 사용자가 보험사별 약관/담보분류표 자료를
사진 또는 텍스트로 제공하면서 신규 데이터를 계속 추가할 예정이다.

작업 원칙:

예를 들어 사용자가

"롯데손해보험에 담보 추가"

를 요청하면:

1. 롯데 자료만 분석
2. lotte.ts만 보험 데이터 변경 대상으로 허용
3. 필요한 경우 index.ts / TermsMaster.tsx에는
   신규 탭 연결에 필요한 최소 코드만 추가
4. 다른 보험사 파일 수정 금지
5. 기존 데이터 재정렬 금지
6. 기존 담보명/코드/그룹 수정 금지
7. 작업 후 diff 확인
8. 보험 데이터 자동 변경 감지 검사 실행
9. build 및 TypeScript 검사
10. 변경 파일을 사용자에게 보고

사용자가 첨부한 자료에 없는 내용을
AI가 임의로 추가해서는 안 된다.


## 11. GitHub 운영 원칙

중요한 보험 데이터 변경 전후에는 Git diff를 확인한다.

정상 기준점:

`insurance-data-stable-2026-08-24`

향후 대규모 데이터 업데이트가 정상 완료되면
새로운 정상 기준점을 추가할 수 있다.

기존 정상 기준점은 삭제하지 않는다.


## 12. 새 AI 채팅에서 가장 먼저 할 일

새로운 ChatGPT/AI 채팅에서 보험브릿지 작업을 이어갈 경우:

1. PROJECT_STATUS.md 확인
2. AGENTS.md 확인
3. 현재 GitHub 코드 상태 확인
4. 사용자의 현재 요청 범위 확인
5. 수정 전 변경 대상 파일 확정

이 과정을 거친 후 작업한다.

이 문서의 내용보다 실제 GitHub 코드가 최신일 수 있으므로
코드 관련 사실은 현재 저장소를 확인한 뒤 판단한다.

단,
보험 데이터 보호 원칙은 항상 유지한다.


## 13. 현재 다음 작업

하나손해보험 담보별 분류표 추가 완료:
- 통합암(유사암제외) (9개 그룹)
- 고액치료비암 (5개)
- 11대특정암 (11개)

현재 다음 주요 작업:

`보험사별 담보분류표 신규 자료 입력 및 사이트 확장`

네이버 KB손해보험 검색 노출 문제는
현재 코드 수정 없이 색인 진행 상태를 관찰한다.

Google Analytics는 데이터 수집을 시작한 상태이므로
향후 방문 통계가 누적되면 분석한다.
